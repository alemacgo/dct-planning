(so-forall (?T 1 @ist)
    (exists (?y @cls)
        (forall (?x @var)
	        (and
	            (or (not (?N ?x ?y))
	                 (?T ?x)
	            )
	            (or (not (?P ?x ?y))
	                (not (?T ?x))
	            )
	        )
        )
    )
)    
